---
type: algorithm
slug: alg-backoff-on-floodwait
factory: mailtg-bridge
status: draft
owner: Евгений
updated: 2026-07-14
form: decision-table
sidecar_format: dmn-1.4
sidecar: alg-backoff-on-floodwait.dmn
produced_by: migrate
---
# Алгоритм: отступ при FloodWait и преходящих ошибках

## Контекст применения
Применяется в -> cmp-bridge-orchestrator/cap-apply-backoff при ошибках внешних сторон в любом такте
(-> scn-inbound-collect-cycle, -> scn-outbound-reply, -> scn-control-command, -> scn-session-invalid-alert).
Реализует требование устойчивости -> rules-integrity и -> nfr-operability (NFR-OPS-06).

## Вход
Класс ошибки внешней стороны от -> cmp-tg-gateway/cap-surface-session-errors или почтового контракта:
FloodWait (с указанным Telegram интервалом ожидания) / преходящая (сеть, временный отказ IMAP/SMTP) /
сессия недействительна-отозвана.

## Выход
Решение: «выдержать отступ и продолжить на следующем такте» либо «остановить опрос (блокирующее)».

## Правила
Форма — **таблица решений** (DMN), машинный сайдкар `alg-backoff-on-floodwait.dmn`. Hit policy **FIRST**
(классы взаимоисключающие; последняя строка — явный дефолт для неизвестного класса).

| # | error_class | → Реакция |
|---|---|---|
| 1 | FloodWait | не повторять раньше указанного Telegram интервала ожидания; такт затронутого источника пропускается, курсор не меняется |
| 2 | transient (сеть / врем. отказ IMAP/SMTP) | отступ ≥ мин. интервала (нижняя граница из конфигурации, допускается нарастающий); работа на следующий такт |
| 3 | session-invalid (недействительна / отозвана) | НЕ ретраить → `-> scn-session-invalid-alert` (блокирующее состояние, разовое уведомление) |
| 4 | — (иной / неизвестный класс) | как `transient` (строка 2) — безопасный дефолт «отступить и продолжить», не срывать и не терять |

**Hit policy:** FIRST.

Сквозные инварианты отступа (не строки таблицы — см. секцию «Инварианты»): интервал между повторами
≥ заданного отступа (нет busy-loop); на любом отступе сообщения и письма не теряются — непродвинутый
курсор и непотреблённое письмо гарантируют повторную обработку (at-least-once, `-> alg-dedup-idempotency`).

## Порядок проверок
Сначала FloodWait (уважить директиву Telegram по времени), затем преходящая, затем блокирующая сессия;
неизвестный класс → как преходящая (без потери).

## Причины выбора порядка
FloodWait несёт явный интервал ожидания от Telegram и приоритетен; блокирующая сессия не ретраится, чтобы
не зациклиться (-> nfr-operability NFR-OPS-06).

## Инварианты
- Интервал между повторами ≥ заданного отступа (отсутствие busy-loop).
- Потерянных сообщений = 0 при инъекции FloodWait/сетевых ошибок.
- Блокирующее состояние сессии не порождает повторных уведомлений (разовое).

## Связи
- Правила: -> rules-integrity
- Сценарии: -> scn-inbound-collect-cycle, -> scn-outbound-reply, -> scn-session-invalid-alert
- Компоненты: -> cmp-bridge-orchestrator, -> cmp-tg-gateway
